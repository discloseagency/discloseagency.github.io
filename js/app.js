// let body = document.querySelector('body');
// let menuNode = document.querySelector('.header-center');

// function getPageYOffset() {
//       return window.pageYOffset;
// }

// function toggleMenu() {
//       let currentOffset = getPageYOffset();
//       if (currentOffset > 250) {
//             menuNode.classList.remove('display-none');
//       }

//       if (currentOffset < 250) {
//             menuNode.classList.add('display-none');
//       }
// };

// body.onscroll = (event) => {
//       toggleMenu();
// }
